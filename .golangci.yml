version: "2"

linters:
  enable:
    - bodyclose
    - errorlint
    - misspell
    - noctx

formatters:
  enable:
    - gofmt
    - goimports
  settings:
    gofmt:
      simplify: true
    goimports:
      local-prefixes:
        - unreal-review
