version: "2"

linters:
  enable:
    - bodyclose
    - depguard
    - errorlint
    - misspell
    - noctx
  settings:
    depguard:
      rules:
        findings:
          files:
            - "**/internal/findings/**"
          allow:
            - $gostd
        review:
          files:
            - "**/internal/review/**"
          allow:
            - $gostd
            - unreal-review/internal/findings
            - github.com/odvcencio/gotreesitter

formatters:
  enable:
    - gofmt
    - goimports
  settings:
    gofmt:
      simplify: true
    goimports:
      local-prefixes:
        - unreal-review
